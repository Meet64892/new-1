# Base URL for the backend API.
# Leave empty to use the Vite dev-server proxy (recommended for local development).
# For production builds, set this to your deployed backend URL, e.g. https://api.example.com
VITE_API_BASE_URL=
